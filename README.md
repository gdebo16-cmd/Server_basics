# Express server basics

Small Node/Express + EJS app used to learn routing, static files, and views.

## Run

```bash
npm install
npm start          # node server.js, port 3001
npm run devStart   # nodemon
```

## Layout

`server.js` · `routes/users.js` · `views/` · `Public/`
